{
  "name": "quay",
  "version": "0.1.0",
  "description": "Small keydown handler for accurately assessing keypresses",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepack": "npm run build",
    "prebuild": "rm -rf build.js",
    "build": "babel index.js > build.js",
    "preexample": "npm run build",
    "example": "browserify -d -t babelify example/index.js > example/bundle.js"
  },
  "keywords": [
    "keys",
    "keyboard",
    "keypress",
    "keydown",
    "keyup"
  ],
  "author": "Matt Styles",
  "license": "ISC",
  "devDependencies": {
    "babel": "^5.8.21",
    "babelify": "^6.1.3",
    "browserify": "^11.0.1"
  },
  "dependencies": {
    "core-js": "^1.0.1",
    "eventemitter3": "^1.1.1",
    "raf-stream": "^3.0.0",
    "vkey": "^1.0.0"
  }
}
